---
description: Close out the current session — verify, document, and report.
---

1. Confirm the current task's acceptance criteria are met and verified (build + real run), not just assumed.
2. Run `/document` to sync `PROJECT_STATE.md`, `HANDOFF.md`, and `NEXT_TASK.md`.
3. Confirm `NEXT_TASK.md` contains exactly one clear next task.
4. Do not start new work after this point.

Report only: files modified, files created, build result, verification, blocking issues. Then stop.
